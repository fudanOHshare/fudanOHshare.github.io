---
title: Git ssh 443端口连接
date: 2025-11-09 19:00:00 +/-0000
categories: [Problem Solution, Git]
tags: [实用工具]     # TAG names should always be lowercase
description: 404 not found~~~~~~
comments: false
math: true
media_subpath:
---

# Github 新仓库 + SSH(443) 工作流文档

* 在 GitHub 上创建新仓库

* 打开 [GitHub](https://github.com/) → 登录账号。

* 点击右上角 **+ → New repository**。

* 填写仓库名，例如 `ComputerVision`。

* 选择是否 Public / Private。

* 点击 **Create repository**。

👉 此时 GitHub 会给你两个连接方式：

* HTTPS: `https://github.com/用户名/仓库名.git`

* SSH (默认22端口): `git@github.com:用户名/仓库名.git`

***

* 本地初始化项目并绑定远程

* 在本地打开 PowerShell / CMD，进入项目目录：

```powershell
cd E:\ComputerVision
```

1. 初始化 Git 仓库（如果还没初始化）：

```powershell
git init
```

1. 添加远程仓库（先用默认 SSH 格式）：

```powershell
git remote add origin git@github.com:用户名/仓库名.git
```

***

* 修改 SSH 到 443 端口

由于你所在网络的 22 端口被屏蔽，需要手动改成 443：

1. 修改远程地址：

```powershell
git remote set-url origin ssh://git@ssh.github.com:443/用户名/仓库名.git
```

1. 确认修改结果：

```powershell
git remote -v
```

1. 输出应类似：

```plain
origin  ssh://git@ssh.github.com:443/用户名/仓库名.git (fetch)
origin  ssh://git@ssh.github.com:443/用户名/仓库名.git (push)
```

***

* 提交并推送代码

* 添加文件到 Git：

```powershell
git add .
```

1. 提交：

```powershell
git commit -m "首次提交"
```

1. 推送到 GitHub：

```powershell
git push -u origin main
```

> 如果分支还不是 `main`，先改名：
>
> ```powershell
> git branch -M main
> ```

***

* 测试 SSH(443) 是否正常

运行：

```powershell
ssh -T -p 443 git@ssh.github.com
```

如果输出：

```plain
Hi 用户名! You've successfully authenticated, but GitHub does not provide shell access.
```

说明连接成功 ✅

